# Thin Grok TUI user rules

Paste **this file** into Grok user rules. Do **not** paste the full text of
`~/.grok/rules/*.md` — those already load as always-on rules. Duplicating them
doubles input tokens every turn.

```
Follow ~/.grok/rules/caveman.md, rtk.md, token-efficiency.md, and vibe-coding.md.

Coding sessions: grok-mcp-coding (Serena + Headroom only).
Personal mail/calendar: grok-mcp-personal.

Done means: affected tests green + diagnostics clean + light self-check.
Do not spawn an in-session multi-reviewer panel (pre-commit does that).
```
