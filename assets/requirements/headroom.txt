# Headroom proxy + RTK installer + structural search
# Floors = latest verified on 2026-08-27. ast-grep 0.44.1 is excluded by headroom-ai.
# 0.35 502'd Grok /v1/responses SSE (TUI Retrying). 0.36 adapts those 200 streams.
# [proxy] extra is required in 0.36 (CLI refuses to start without it).
# tokenizers 0.23.1 breaks transformers 5.13.x and Headroom's proxy check.
# mcp extra stays <2 (headroom-ai[proxy] requires mcp>=1.28.1,<2.0.0).
headroom-ai[proxy]>=0.36.5
tokenizers>=0.22.0,<=0.23.0
ast-grep-cli>=0.45.2
